/* it('write what we expect a given function to do', function() {
    expect(some_function(argument)).toEqual(expected_outcome)
}) */

it("should shuffle the array", function() {
    expect(1+1).toEqual(2);
})